# Extensions

Place project or global extensions here:

- `extensions/name.ts` is auto-discovered
- `extensions/name/index.ts` is auto-discovered

Examples under `examples/` are not loaded automatically.
