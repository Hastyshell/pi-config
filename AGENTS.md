# Pi Coding Agent Instructions

This file is loaded automatically by Pi as context. Use it for global or project-level collaboration preferences.

## Default Preferences

- Respond in Chinese by default, unless the user explicitly requests another language.
- Read the relevant context before editing files.
- After making changes, summarize the touched file paths and the result.
- Ask clarifying questions when requirements are ambiguous; do not expand the scope without confirmation.
